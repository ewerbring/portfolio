"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var lodash_1 = require("lodash");
var allRange = [0, 65535];
var latinRange = [32, 255];
function charByRange(charRange) {
    return String.fromCharCode(lodash_1.random.apply(null, charRange));
}
function strings(length, charRange) {
    if (length === void 0) { length = lodash_1.random(1, 100); }
    if (charRange === void 0) { charRange = allRange; }
    return lodash_1.range(length)
        .map(function () { return charByRange(charRange); })
        .join('');
}
function everything(length) {
    return strings(length, allRange);
}
exports.everything = everything;
exports.default = everything;
function latin(length) {
    return strings(length, latinRange);
}
exports.latin = latin;
